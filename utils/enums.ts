export enum STATUS {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum POPULAR {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum ROUTE {
    CATEGORY = 'category'
}

export enum FORMAT {
    DATE_TIME = 'DD-MM-YYYY HH:mm'
}

export enum MESSAGE {
    SUCCESS = 'success',
    ERROR = 'error'
}

export enum PAGE {
    CURRENT = 1,
    SIZE = 10
}
